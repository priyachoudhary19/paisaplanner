services:
  - type: web
    name: aaexpense
    env: python
    branch: main
    rootDirectory: project
    buildCommand: build.sh
    startCommand: gunicorn project.wsgi:application
    pythonVersion: "3.13"
